# 获取操作员角色列表

用来添加操作员工字段中的role角色列表

### 请求语法

```
GET v2/signature/operator_role
```

### 请求头

| 名称 | 类型|是否必填 |示例值| 描述|
|---|---|---|---|---|
| Authorization | 字符串|是|--| 授权TOKEN |


### 响应元素

| 名称 | 类型 |示例值| 描述|
|---|---|---|---| 
| data | 对象|--| 返回数据，详细查看data响应 |
| status | 数字|200| 请求状态；200=请求成功；400=请求失败 |
| msg | 字符串|success| 请求文字描述 |

### data响应

| 名称    | 类型 |示例值| 描述    |
|-------|---|-|-------| 
| value | 字符串|是| 角色id值 |
| label | 字符串|是| 角色名称  |


### 返回数据
```
{
    "status": 200,
    "msg": "ok",
    "data": [
        {
            "value": "0352145ee52824b2480d4443ace45bd6",
            "label": "智能工具使用员"
        },
        {
            "value": "0bb343548a5f060b791157b61f517f8f",
            "label": "部门管理员"
        },
        {
            "value": "14f837702232cd89f8fbfe62025f6758",
            "label": "IT信息管理员"
        },
        {
            "value": "52ec6af412793631e3ca2e7f72683705",
            "label": "业务员"
        },
        {
            "value": "716fdc2046f1e7d552af950347509a5a",
            "label": "企业合同管理员"
        },
        {
            "value": "8c45202fa2a228ba5de1c958adfcdd00",
            "label": "用印审批岗"
        },
        {
            "value": "aec6db8af94b159a7f38498244985d07",
            "label": "企业模板管理员"
        },
        {
            "value": "e8488afc780f290ccb134e4731229ebc",
            "label": "超级管理员"
        },
        {
            "value": "f2a4100a7bdedb083b3b567aa9263b70",
            "label": "智能工具配置员"
        },
        {
            "value": "f34d530a02133064178128ffa94cd2eb",
            "label": "企业印章管理员"
        }
    ]
}
```

### 请求示例

<code-group>
<code-block title="PHP" active>

```php
<?php

$token = 'your access_token';

$ch = curl_init();

curl_setopt_array($ch, [
    CURLOPT_URL => 'http://sms.crmeb.net/api/v2/signature/operator_role',
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer-' . $token,
        'Content-Type: application/json',
    ],
]);

$response = curl_exec($ch);
curl_close($ch);

var_dump(json_decode($response, true));
```

</code-block>
<code-block title="Java">

```java
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

String token = "your access_token";

HttpRequest request = HttpRequest.newBuilder()
        .uri(URI.create("http://sms.crmeb.net/api/v2/signature/operator_role"))
        .header("Authorization", "Bearer-" + token)
        .GET()
        .build();

HttpResponse<String> response = HttpClient.newHttpClient()
        .send(request, HttpResponse.BodyHandlers.ofString());
System.out.println(response.body());
```

</code-block>
<code-block title="Node">

```javascript
const token = 'your access_token';

(async () => {
    const response = await fetch('http://sms.crmeb.net/api/v2/signature/operator_role', {
        headers: {
            'Authorization': 'Bearer-' + token
        }
    });

    const data = await response.json();
    console.log(data);
})();
```

</code-block>
<code-block title="Go">

```go
package main

import (
	"fmt"
	"io"
	"net/http"
)

func main() {
	token := "your access_token"

	req, _ := http.NewRequest("GET", "http://sms.crmeb.net/api/v2/signature/operator_role", nil)
	req.Header.Set("Authorization", "Bearer-"+token)

	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		panic(err)
	}
	defer resp.Body.Close()

	result, _ := io.ReadAll(resp.Body)
	fmt.Println(string(result))
}
```

</code-block>
</code-group>
